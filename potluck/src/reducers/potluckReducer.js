

const initialState = {
    isLoading: false,
    potlucks: [],
    errors: ''
}

const potluckReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}